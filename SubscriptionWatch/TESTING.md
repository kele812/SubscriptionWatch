# v3.1 验证记录

2026-09-16，Windows / Node.js 24.18.0。本地临时SQLite、HTTP端口和模拟外部服务测试。

- 首次设置竞态、无会话/伪造会话拒绝、登录页与后台分离、禁止注册、退出及改密失效、禁止缓存。
- 面板数据和密钥隔离、签名、去重、批量事务失败回滚。
- 三种风险规则、白名单、滚动过期、手动取消后只评估新异常、独立清理与CSV转义。
- 模拟TG私聊绑定、非绑定用户拒绝、取消确认。
- 配置导入鉴权与格式校验、密钥不回显、模拟401错误提示。
- 官方测试MMDB格式安装、SHA校验、失败回滚、更新清理与重启读取。
- v2迁移及v3.0单账号迁移保留数据。

浏览器检查登录、访问记录、系统设置和390px手机布局；仅使用本地测试账号和模拟记录。

未连接你的VPS，不能据此确认旧部署曾泄漏数据。本机没有Docker/PHP运行环境，未执行真实镜像部署、Xboard联调、TG消息或MaxMind授权下载。

MMDB测试数据来源：https://github.com/maxmind/MaxMind-DB/tree/main/test-data ，仅供测试，许可证见test/fixtures。

## v3.2

13组本地测试通过。新增双机器人独立绑定、通知路由、同名用户查询隔离、跨机器人确认拒绝、独立移除，以及旧配置选中面板/首面板迁移和幂等验证。Telegram使用模拟API，未发送真实消息。

## v3.3

新增：TG超时与数字错误码不终止服务/会话；白名单错配拒绝与只显示主动添加项；邮箱变化撤销豁免；三级风险与到期降级；中国大陆IP和数据中心IP计数；Cloudflare/IP请求豁免不删除记录；定时清理保留风险/评估历史；Token+个人ID校验；封禁默认关闭、管理员及邮箱错配拒绝、单用户写入、邀请关系保留、回读验证与不重复执行。外部服务全部模拟，没有真实封禁或TG发送。浏览器检查新增规则字段、简化白名单、个人ID配置、无文字/密码的清空确认框、原始UA详情。

最终结果：22组自动测试通过；前端选择器检查通过，浏览器未记录脚本错误。

## v3.4

23组当前Node测试通过，API封禁测试已替换为插件控制协议测试。控制请求签名与响应签名、随机nonce、防重放、跨面板结果隔离、任务仅下发一次、默认关闭/升级关闭、结果回执重传、过期未知与迟到结果、旧管理凭据删除。PHP8.5.10执行test/plugin-control.php：模拟Xboard模型、事务、Redis和HTTP，验证不可信响应拒绝、重复执行拒绝、用户身份保护、过期任务、正常封禁仅改变状态并清理会话、异常回滚。所有PHP文件通过PHP8.2解析与PHP8.5 lint。没有真实Xboard/Redis集成测试或Docker构建。

## v3.5

29项Node测试通过；新增自有节点部分豁免、证据明细、观察期重置与重启、手动解封能力校验及幂等、通知合并、健康状态、包含Geo库与密钥的备份恢复/损坏拒绝。PHP8.5模拟测试验证封禁与解封的身份保护、回滚和字段保留；四个PHP文件语法检查通过，另经PHP8.2解析。浏览器使用本地虚构账号验收登录、节点设置保存、观察期保存、风险证据卡片、解封确认/旧插件拒绝及备份入口。未连接真实Xboard或Telegram，也未执行Docker构建。

## v3.6

33项Node测试通过，新增国内IP次数去重/白名单/未知IP、手动封禁鉴权与观察期旁路、网页导入无需重启/会话失效/采集继续、损坏备份及不匹配密钥回滚。浏览器用本地虚构数据执行上传、确认导入、返回登录、原账号重新登录；未操作生产环境。插件协议保持v3.5兼容。

## v3.7

37项Node测试通过：新增成功状态过滤、完整UA双条件、跨国家豁免、共同窗口组合、无副作用预览、历史条件快照、纯双条件自动封禁、旧样本状态迁移。浏览器使用本地虚构数据验证动态条件、预览、保存和详情首框IP/UA，控制台无错误；未访问或修改生产VPS。
